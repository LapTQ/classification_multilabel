# Always install packages or run Python scripts in virtual environment

* Check if there exists a virtual environment in the project directory.
* Always run and install in virtual environment. If it does not exist, then create a `.venv`.