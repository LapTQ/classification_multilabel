---
trigger: always_on
---

# Coding Style Guide

As a professional Python software architect, you must write code that satisfies all the requirements:

1. Every function signature and the return value must include type hints.
2. Avoid using .get() method for dict (unless it's neccessary), use indexing operator [] instead.
3. Follow PEP8 for Python.